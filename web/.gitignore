node_modules
dist
dev-dist
*.local
.env
.env.*
!.env.example

# TypeScript incremental build state — regenerated by every `tsc -b`, so tracking
# it dirties the tree on every build. mobile/.gitignore has always had this rule;
# web/ did not, and the two files it produces were tracked until HZ-002.
*.tsbuildinfo

# lint and test caches
.eslintcache
coverage/

# generated PWA icons (run `npm run gen-icons`)
public/icon-192.png
public/icon-512.png
public/apple-touch-icon.png
